import { Router } from "express";
import { orderItemController } from "../controllers/orderItem.controller";

const router = Router();

// router.post('/add/:pid',orderItemController.create);

export default router;